# 08. 함수와 숫자 입력 검사

## 이번 파일에서 할 일

- [ ] `get_menu_choice()` 함수를 만든다.
- [ ] 입력값에 `.strip()`을 사용해 앞뒤 공백을 없앤다.
- [ ] `int()`로 숫자로 바꾸고 1~5인지 검사한다.
- [ ] 빈 입력, 글자 입력, 1~5 밖 숫자에는 안내하고 다시 입력받는다.

## 힌트

`int("abc")`는 오류가 날 수 있으므로 `try`와 `except ValueError`로 감싼다.

## 완료 기준

`abc`, 빈 Enter, `9`, ` 1 `을 각각 입력해도 프로그램이 멈추지 않는다.

## 다음 파일

[09_Quiz_클래스.md](09_Quiz_클래스.md)
