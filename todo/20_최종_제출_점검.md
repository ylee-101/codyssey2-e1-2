# 20. 최종 제출 점검

## 이번 파일에서 할 일

- [ ] `python3 main.py`가 정상 실행되는지 마지막으로 확인한다.
- [ ] `state.json`에 `quizzes`와 `best_score`가 있는지 확인한다.
- [ ] `Quiz`와 `QuizGame` 클래스가 있는지 확인한다.
- [ ] `git log --oneline`에서 의미 있는 커밋이 10개 이상인지 확인한다.
- [ ] `git status`에 의도하지 않은 파일이 없는지 확인하고, 필요한 변경을 커밋·푸시한다.
- [ ] README 필수 항목과 스크린샷을 확인하고 GitHub 저장소 URL을 제출한다.

## 완료 기준

프로그램, GitHub 저장소 URL, 필수 스크린샷을 모두 제출할 수 있다.

축하합니다. 과제를 완료했습니다.
